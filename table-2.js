import {a} from './multiplier.js';
import {b, c, d, e, f, g, h, i, j, p} from './numbers.js';
import {multi1, multi2, multi3, multi4, multi5, multi6, multi7, multi8, multi9, multi10} from './multi.js';

const sickLentele = `
        Daugybos lentele:
        ${a} * ${b} = ${multi1}
        ${a} * ${c} = ${multi2}
        ${a} * ${d} = ${multi3}
        ${a} * ${e} = ${multi4}
        ${a} * ${f} = ${multi5}
        ${a} * ${g} = ${multi6}
        ${a} * ${h} = ${multi7}
        ${a} * ${i} = ${multi8}
        ${a} * ${j} = ${multi9}
        ${a} * ${p} = ${multi10}

`;

export {sickLentele};